services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: transformer-ids-backend:cuda
    container_name: transformer-ids-backend
    ports:
      - "8000:8000"
    gpus: all
    restart: unless-stopped
